require 'spec_helper'

describe CicEmbededPayment do
  it 'has a version number' do
    expect(CicEmbededPayment::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
